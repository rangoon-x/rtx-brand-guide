# Rangoon.Tech Brand CDN

Versioned static brand assets for the Rangoon.Tech brand guide app.

Current package:

- `v1/` - finalized brand guide assets and design source of truth.

GitHub Pages test URL:

```txt
https://rangoon-x.github.io/rtx-brand-guide/v1
```

Publish the version folder as immutable CDN content. Do not overwrite files inside an existing version after release; create `v2/` for future brand revisions.
